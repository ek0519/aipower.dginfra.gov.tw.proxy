services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-power:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "3000"
    ports:
      - "3001:3000"
